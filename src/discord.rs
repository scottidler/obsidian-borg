use crate::config::{Config, DiscordConfig};
use eyre::Result;
use std::sync::Arc;

pub async fn run(_token: String, _dc_config: DiscordConfig, _config: Arc<Config>) -> Result<()> {
    // Implementation in Phase 4
    Ok(())
}
